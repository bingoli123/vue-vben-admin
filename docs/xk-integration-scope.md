# Vben 原生页面接入范围

状态：原生组件接入已实现，接口测试、类型检查与生产构建通过。浏览器验收结果单独记录，不以构建代替真实页面验收。

## 已明确范围

- 在当前 vue-vben-admin 仓库实施，复用 Vben 原生登录、布局、图标、表格、表单、抽屉、弹窗及权限能力。
- 现有后端接口决定业务字段、查询参数、返回数据、授权和数据范围；必要时为表单增加输入项。
- 不以 xk-backend/admin-web 的页面替换 Vben 页面，不修改旧前端。
- 首期接入已有后端功能：登录退出、当前身份、动态菜单、按钮权限、单位、用户、角色、菜单管理、角色功能及单位授权、用户角色绑定、锁定、重置密码、本人资料查看与改密、报表。
- 清除正式应用左侧演示菜单及对应演示数据和专用源码；共享组件仍按实际依赖保留。
- 沿用此前已明确的范围：不建设通知中心、保留空白仪表盘、暂不建设干部考核业务页面。
- 页面已有但后端未实现的能力，先确认范围再补接口。

## 已核对事实

- `playground` 包含完整的系统用户、角色、菜单、部门页面，使用 antdv-next 及 Vben 的 Page、Form、VxeGrid、Drawer、Tree；`apps/web-antd` 是使用 ant-design-vue 的基础应用。
- 后端菜单支持 `icon` 和 `activeIcon`，保存参数、实体、管理返回及导航返回均已接入原版 IconPicker；`shortcutIcon` 按后续要求从前后端及数据库删除。
- 原版菜单的 `keepAlive`、`affixTab`、隐藏配置、徽标、外链及内嵌页配置，在现有后端菜单模型中没有一一对应的可持久化字段。
- 后端采用 `code=SUCCESS` 响应、UUID Token、RSA-OAEP/SHA-256 密码加密；Vben 的请求层和认证状态层需要适配，不能沿用演示的 code=0、明文密码和 refreshToken 协议。
- 角色功能与单位授权已经有原子保存接口；管理员身份、菜单访问、按钮权限、单位范围保持分别处理。
- 报表菜单列表和打开接口已存在，打开后使用同源 Cookie；报表凭据不进入 URL。

## 已确认决策

| 编号 | 决策 | 结论 |
| --- | --- | --- |
| Q1 | 正式应用基线 | 用户委托选择，以包含完整原生管理页面的 playground 为基线，保留 Vben 样式与组件 |
| Q2 | 菜单高级配置 | 新增并保存 activeIcon；按后续确认删除整组未接入后端的高级设置控件 |
| Q3 | 登录入口 | 保留账号密码、记住用户名及原生滑块，补服务端一次性校验；删除其他登录和自助注册入口 |
| Q4 | 个人中心 | 保留资料查看和本人改密；允许本人修改姓名、性别和头像，其他资料由管理员维护；删除其他安全设置 |

后端以独立接口白名单限制本人资料编辑，不允许修改管理员身份、单位或角色。滑块校验凭据短时有效且一次性消费，不复用密码公钥。

## 暂存基线

前一轮错误方向的迁移已保存为 Git stash 对象 `1c578cc5de66df474210affbfa4e91b5f7266e48`；验收截图保存为 `187cd1bdd12c4d50853babd10d4403d09db70876`。当前源码从上游提交 `df014ae7` 重新分析，不直接恢复该迁移。

## 实现与启动

正式应用使用 `playground`，根目录 `pnpm dev` 已指向该应用，端口为 5666；`pnpm build:xk` 构建到 `playground/dist`。保留其他上游应用作为仓库原有内容，它们不是当前正式应用入口。

开发默认将 `/api` 与 `/ureport` 代理到 `http://127.0.0.1:8080`。指定隔离后端可执行 `XK_BACKEND_URL=http://127.0.0.1:18080 pnpm dev`。生产环境需将这两个路径同源代理到后端、保留 Host/Origin，并为前端 history 路由配置 index.html 回落。生产使用 HTTPS，浏览器密码加密依赖安全上下文。

后端需运行本轮新版代码及 Liquibase 010/011/012/013 迁移。旧服务没有滑块和个人资料新接口，不能与新版前端配套使用；旧 admin-web 未适配必填 captchaToken。

原生管理表单根据后端字段增加输入项；菜单使用图标与激活图标两个独立 IconPicker；快捷图标从控件、请求、返回与数据库列一起删除。功能权限树按父子联动：部分子项选中时上级半选，全部子项选中时上级全选；保存包含必要上级菜单，不增加未选兄弟按钮。单位数据范围沿用后端有效下级规则，缺省授权组不修改，保留历史失效项回显和移除。菜单 ID 决定路由身份，普通页面仅映射已实现组件；报表页面通过后端打开接口获得同源地址。

## 验证记录

- `pnpm exec vitest run --config playground/vitest.config.ts`：7 项通过，覆盖导航映射、字段白名单、状态/锁定/删除方法、授权组和完整候选分页。
- 原生滑块组件测试：4 项通过，覆盖等待服务端、拒绝、重置后的过期响应、禁用状态。
- `pnpm exec vue-tsc --noEmit --skipLibCheck -p playground/tsconfig.json`：通过。
- `pnpm build:xk`：通过。上游打包目标对 BigInt 存在提示，运行环境需现代浏览器。
- 后端默认单元测试通过；登录/滑块相关 17 项定向集成测试通过；导航、报表、续期、统一菜单、用户和个人资料另外 20 项定向集成测试通过，包括报表查询与导出及头像上传读取。
- 浏览器已确认原生登录页正常渲染；本地 API smoke 已用隔离账号完成 challenge、RSA 登录、身份、菜单、四类管理、资料、权限、数据范围和报表菜单请求。真实拖动滑块后的浏览器登录、管理表单交互和窄屏管理页面仍需完成验收，不能记为已通过。

本机正式服务为 5666 前端与 8080 后端，后端连接 xk 库。测试另使用独立 _test 数据库。滑块轨迹为轻量交互校验，不能视作强机器人识别方案。


## 2026-09-16 管理页面反馈修正

- 用户单位树关闭无文字的展开工具栏，保留真实单位层级与“全部单位”。
- 性别改为原生 Select，增加性别及所属单位占位提示，选中单位后新增用户默认带入该单位。
- 用户新增手机号必填（11 位大陆手机号）、工号可选；全拼和考勤号从输入、查询、列表、详情、个人中心及前后端契约删除，数据库通过 013 迁移物理删除对应列。历史账号缺手机号可暂不补齐。
- 抽屉统一字段宽度、标签宽度与响应式单双列，说明独占一整行；高级设置区块按后续要求删除。
- 用户详情时间和单位创建时间使用 Vben formatDateTime，按浏览器时区显示 YYYY-MM-DD HH:mm:ss。
- 单位状态为已启用/已禁用；角色列表原生 Switch 支持权限控制、确认、加载态和服务端成功后更新版本。
- 菜单类型显示蓝色目录、默认色菜单、紫色按钮；图标和激活图标使用随包注册的 Lucide 图标，原生选择器不再依赖外部图标请求。
- 13 项前端测试通过，包含真实 Tree/TreeSelect/Select 控件与断网 SVG 图标渲染、搜索及选中回填；类型检查与生产构建通过。
- 后端默认单元测试和 33 项用户、资料、单位、角色、密码及数据范围集成测试通过，均使用独立测试库。

- 后续菜单表单调整：新增下级置于行内操作；普通菜单业务页面改为手动输入且必填；删除高级设置标题及其激活路径、链接地址、徽标、缓存、固定标签和隐藏相关控件。

- 角色功能权限联动验收：真实授权抽屉组件覆盖子选父半选、子项全选、父选全部下级、取消勾选、保存与重新打开，以及点击半选父项补全子项。修复 Tree 在回填时把原始节点误按 item.value 取 ID、导致半选父项被当成全选的问题。

- 删除字段贯穿前端、后端和数据库：移除 `fullPinyin`、`attendanceNo`、`shortcutIcon`；013 迁移删除 sys_user 两列及 sys_menu/sys_module 的 shortcut_icon。高级设置从未增加后端字段，无对应数据库列。旧迁移保留原内容以维持校验和与升级链。
- 字段删除验证：16 项前端测试、类型/规范检查与生产构建通过；后端默认单元测试和独立测试库的 19 项集成测试通过。本机 xk 备份后已执行 013 迁移并重启后端；保留字段及菜单授权数据摘要一致，健康检查和运行时接口文档已核对。
- 新增/编辑刷新：用户、角色、单位、菜单表单在保存成功后的 onClosed 中通知父页面，调用 gridApi.reload 重新查询并重置分页；取消不刷新。真实 connectedComponent 抽屉连接测试覆盖四类表单及再次打开取消，追加真实列表组件的新建/编辑测试，验证四类页面重新查询并显示新数据，共 28 项前端测试、类型和规范检查通过。
- 用户左侧单位筛选包含所选单位及全部下级用户，由后端按 tree_path 筛选并与当前账号数据权限求交。切换单位或全部单位调用 reload 重置第一页并保留其他查询条件；29 项前端测试、类型/规范检查与 24 项后端集成测试通过，后端已重启生效。
- 移除正式应用的全局搜索、语言切换、时区切换和锁屏，以及偏好设置、登录工具栏和快捷键中的对应入口。启动时定向覆盖这些功能的旧缓存并清理旧锁屏状态；重置偏好后同样保持关闭。继续复用 Vben 原生布局，其余个人偏好保持不变。这些功能没有业务接口或数据库字段。


## 2026-09-16 数据字典与字典项维护（任务 01）

- `/admin/dictionaries` 映射正式字典双列表页面，两侧独立查询、分页，字典名称/类型/备注与项标签/键值/排序/备注均可维护。
- 使用既有菜单与按钮授权；表单读取最新详情和版本，保存成功关闭后刷新，取消不保存。切换字典重建右侧状态，删除或翻页使所选字典不可见时清空右侧。
- 类型、键值允许改删，不增加业务引用保护；键值按字符串保留前导零。后续缓存、消费页面与导出分别由任务 02/03 实施。
- JDK 21 后端 80 项默认单元测试及独立 `xk_dictionary_test` 库的 5 项 `DictionaryAdminIT` 通过；前端类型检查、ESLint 与生产构建通过。
- `playwright.dictionary.config.ts` 的真实浏览器用例通过：滑块登录、菜单进入、两侧增改删、取消、搜索/重置、两侧 21 条样本翻页及切换字典重置。只删除用例自建字典并退出会话；未记录凭据。
- 本次浏览器验收使用独立 `xk_dictionary_ui_test` / 18201 / 5761。日常 8080 服务未在本任务重启；未部署或推送。


## 2026-09-16 字典缓存与现有页面接入（任务 02）

- 用户性别的输入、筛选、列表、详情与个人资料使用 `sys_gender`；单位类别编辑、列表和树筛选使用 `sys_unit_category`。单位筛选保留匹配单位的祖先作为层级上下文。
- 通用 DictionarySelect 通过已登录选项接口读取后端缓存，每次挂载/展开重新获取；列表查询与详情打开同步读取。名称转换无法匹配时显示原 key，选择器把已有未知值保留为选项，加载、改名、改键或删除不会自动清空业务值。筛选允许输入未知 key。
- 字典项工具栏“刷新缓存”使用独立 `platform:dictionary:refresh` 权限；后端成功响应后显示结果。未新增可编辑的程序枚举。
- 后端字典事务提交后立即及延迟失效、改类型处理旧新键；005 迁移仅映射明确文字，保留 `222`、空串与 NULL，性别扩为 64 字符。
- 独立 18202/5762 服务和 `xk_dict02_ui_test` 通过真实浏览器：未知 `222` 的列表/详情/编辑、个人资料选项保存、标签改名、改键和删除后原 key 保存、单位类别保存筛选及手动刷新；用例恢复自己的测试种子/个人资料并删除自建单位。后端 80 项单元及 31 项相关集成测试通过；前端类型、生产构建和字典控件/旧表单回归通过。
- 验收配置为 `playground/playwright.dictionary-consumption.config.ts`，凭据只经环境注入。不替换日常 8080 服务，不迁移开发库、不推送。

## 2026-09-17 操作日志导出与手动清理（任务11）

- 日志工具栏分别按 `platform:operation-log:export/delete/clear` 显示按钮；仅有导出或清空权限仍可使用对应动作，查询权限不隐式赋予写权限。
- 导出使用已应用筛选并去除分页，经真实后端生成完整XLSX；JSON、空文件或请求失败不触发下载。
- 选中删除提交ID/版本，翻页、查询或刷新清空选择。确认框冻结当前选择；清空明确针对全系统有效日志，包括筛选之外的数据。成功后重置筛选并回到首页显示本次审计。
- 代码提交 `1c883732`、`a54a9f0b`；前端2项定向测试、类型与提交钩子检查、生产构建通过，后端81项单元与18项定向集成及审查修复后4项清理复验通过。
- 5190代理独立18091及 `xk_operation_log11_browser_test` 副本，未清理任务10原库。当前等待用户在新标签完成滑块登录后进行真实查询/筛选/导出/删除/清空/回查闭环；不能把本段视为已完成页面验收。详细证据在后端 `.scratch/code-management-foundation/evidence/operation-log-management/acceptance.md`。

## 2026-09-17 跨业务关键操作日志覆盖（任务12）

- `fe4d924e` 补齐状态变更、授权、改密、刷新、取号、上传、下载类型；筛选、表格和详情共用 `operationTypes`，对应后端004迁移和51入口动作表。
- 类型检查、提交钩子oxlint/oxfmt/eslint与生产构建通过；沿用已知mini-decimal BigInt目标警告。后端81项单元及157项不同的定向集成通过，覆盖跨业务成功/失败、单位范围和原有管理及报表入口。
- 5190继续代理已更新的18091，隔离浏览器库保持不变。当前工具可用标签仍是登录页；真实页面按人员/模块/类型/状态筛选及按钮验收继续待办，不把接口和构建结果当成UI验收。后端证据 `.scratch/code-management-foundation/evidence/operation-log-coverage/acceptance.md`。
