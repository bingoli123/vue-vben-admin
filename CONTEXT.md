# 新矿平台前端术语

本系统为新矿共享平台提供账号、单位、角色、菜单与报表的管理入口。业务含义与后端领域术语保持一致。

## Language

**本地账号**：由本系统创建和维护的登录账号，是当前用户的身份来源。

**管理员**：系统中固定的管理员身份，拥有全部功能权限及全部单位数据范围。分配一个名称为“管理员”的普通角色不会取得该身份。

**单位**：矿和管理部门的统一组织主体，通过类别区分，可以包含下级单位。 _Avoid_: 部门（作为与单位并存的独立管理对象）

**用户所属单位**：账号绑定的唯一展示单位；它本身不授予数据权限。

**角色所属单位**：角色资料中的描述性文字，不代表角色的数据授权。

**角色单位数据权限**：角色可访问的多个单位及其全部有效下级；普通用户取得全部有效角色范围的并集。空范围表示无权访问单位数据。

**菜单管理**：统一维护目录、普通菜单、报表菜单及按钮权限的管理功能。 _Avoid_: 模块管理、资源权限管理（作为并存的独立维护入口）

**目录**：用于组织下级目录和菜单的导航分组，本身不承载业务页面。

**菜单**：管理员维护的功能导航入口。菜单访问权限与其下的具体操作权限分别授予。

**按钮权限**：普通菜单下的具体操作授权，通过权限标识对应界面操作和业务操作；拥有菜单访问权限不意味着拥有全部按钮权限。

**报表菜单**：指向报表设计器或某张报表的导航入口，下方没有按钮权限。设计菜单授予设计能力，查看菜单授予对应报表的查询、打印和导出能力。

**功能授权树**：角色分配目录、菜单和按钮权限时使用的层级集合，单位数据范围单独配置。
